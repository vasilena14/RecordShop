@model int
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<td class="text-end align-middle">
    <div class="btn-group" role="group">
        @*<a type="button" class="btn btn-primary" href="@Url.Action("Edit/"+item.RecordId)">Edit</a>*@
        <a type="button" class="btn btn-primary rounded-0" asp-action="Edit" asp-route-id="@Model">Edit</a>
        <form method="post" action="Delete">
            <input onclick="if (!confirm('Are you sure you want to delete this item?')){ return false}" type="submit" class="btn btn-danger rounded-0" asp-action="Delete" asp-route-id="@Model" value="Delete" />
        </form>
    </div>
</td>